---
layout: post
title: This is a test
date: 2025-05-17 20:53
categories: [Debug]
tags: [debug]
---

# Test
This is a test for first post which is written by Jekyll static site framework.

```python
def say_hello():
    print('hello everyone from bopin!')
```

```c
if ( SystemInformationClass_1 == SystemTimeSlipNotification )
{
    if ( SystemInformationLength != 8 )
        return STATUS_INFO_LENGTH_MISMATCH;
    if ( PreviousMode && !SeSinglePrivilegeCheck(SeSystemtimePrivilege, PreviousMode) )
        return STATUS_PRIVILEGE_NOT_HELD;
    if ( !(unsigned __int8)PsIsCurrentThreadInServerSilo(
                                v35,
                                SystemInformation,
                                *(_QWORD *)&SystemInformationLength,
                                v8) )
    {
        Handle_1 = (HANDLE)SystemInformation_1->m128i_i64[0];
        if ( Handle_1 )
        {
        Object = 0;
        updated = ObReferenceObjectByHandle(
                    Handle_1,
                    2u,
                    (POBJECT_TYPE)ExEventObjectType,
                    PreviousMode,
                    &Object,
                    0);
        Object_3 = Object;
        if ( updated < 0 )
            return updated;
        }
        else
        {
        Object_3 = 0;
        updated = 0;
        }
        KdUpdateTimeSlipEvent(Object_3);
        return updated;
    }
    return STATUS_ACCESS_DENIED;
}
```